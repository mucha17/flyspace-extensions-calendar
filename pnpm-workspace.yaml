# Local development consumes @flyspace/* from a self-hosted registry, so the public-npm
# recency gate does not apply here.
minimumReleaseAge: 0
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
