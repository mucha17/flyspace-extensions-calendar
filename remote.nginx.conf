server {
  listen 4201;
  server_name _;
  root /usr/share/nginx/html;

  # The shell loads the remote cross-origin (Native Federation), so allow it.
  location / {
    add_header Access-Control-Allow-Origin * always;
    try_files $uri =404;
  }
}
